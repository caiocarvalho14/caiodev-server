services:
  minecraft:
    image: itzg/minecraft-server:latest
    container_name: minecraft_server
    restart: unless-stopped

    ports:
      - "25565:25565/tcp"
      - "19132:19132/udp"

    environment:
      EULA: "TRUE"

      TYPE: PAPER
      VERSION: LATEST
      MEMORY: 4G
      TZ: America/Fortaleza

      MOTD: "CAIODEV SERVER"
      MODE: survival
      DIFFICULTY: hard
      ONLINE_MODE: "TRUE"

      SEED: "6944174826991112"

      PLUGINS: "https://download.geysermc.org/v2/projects/geyser/versions/latest/builds/latest/downloads/spigot,https://download.geysermc.org/v2/projects/floodgate/versions/latest/builds/latest/downloads/spigot,https://github.com/NEZNAMY/TAB/releases/download/6.0.3/TAB.v6.0.3.-.Vanilla.jar"

      ENABLE_ROLLING_LOGS: "true"

    volumes:
      - ./minecraft/data:/data

    networks:
      - minecraft

  #monitor:
  #  container_name: minecraft_monitor
  #  restart: unless-stopped
  #  working_dir: /app
  # command: sleep infinity
  # volumes:
  #   - ./monitor:/app
  # depends_on:
  #   - minecraft
  # networks:
  #   - minecraft

networks:
  minecraft:
    driver: bridge